function submit_new_bill_row_form(e){
  $("#bill_creation_overlay").addClass('overlay_div_component');
  // $(e).parents('form').submit();
  // $(".select_comp").select2('disable');
  // $('#new_bill_row input, #new_bill_row textarea').attr('disabled','disabled');
}
.section-body
  .row
    .col-md-12
      .card
        .card-head.style-primary
          %header.text-xl 
            Claim types
  .card.card-underline
    .card-body
      you've not created a timesheet for this pay period, click 
      = link_to('here', create_timesheet_path(user_id: current_user.id)) 
      to create

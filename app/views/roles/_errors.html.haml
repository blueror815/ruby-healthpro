- if @role.errors.any?
  .alert.alert-callout.alert-danger.no-margin
    %h2= "#{pluralize(@role.errors.count, "error")} prohibited this form from being saved:"
    %ul
      - @role.errors.full_messages.each do |msg|
        %li= msg
- if @instance.errors.any?
  #error_explanation
    .alert.alert-callout.alert-info.no-margin
      %h2= "#{pluralize(@instance.errors.count, "error")} prohibited this " + @model+" from being saved:"
      %ul
        - @instance.errors.full_messages.each do |msg|
          %li= msg
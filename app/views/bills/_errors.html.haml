- if errors.any?
  .alert.alert-callout.alert-danger
    %p= "#{pluralize(errors.full_messages.uniq.count, "error")} prohibited this form from being saved:"
    %ul
      - errors.full_messages.uniq.each do |msg|
        %li.text-xs= msg
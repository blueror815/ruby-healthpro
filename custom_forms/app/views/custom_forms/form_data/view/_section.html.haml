%section.section
  - if section.permissions["#{ current_user.role.short_name }"][action_name] == '1'
    %header
      .text-lg= section.title.humanize
    .section-body
      %dl.dl-horizontal
        - section.section_fields.each do |field|
          = form_field_view(form, field)

